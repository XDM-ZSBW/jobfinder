{
  "version": 2,
  "installCommand": "cd backend && npm install && npm run build && cd .. && node -e \"const api=require('./api/package.json');const root=require('./package.json');root.dependencies={...root.dependencies,...api.dependencies};require('fs').writeFileSync('package.json',JSON.stringify(root,null,2))\" && npm install --production",
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/test",
      "dest": "/api/test.js"
    },
    {
      "src": "/api/voice/(.*)",
      "dest": "/api/index.js"
    },
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "/frontend/dist/$1"
    }
  ]
}
